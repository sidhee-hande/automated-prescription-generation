version: '3.8'

services:
    api:
        build: ./backend
        ports: 
            - 5000:5000

    client:
        build: ./frontend
        volumes:
            - '/app/node_modules'
        ports:
            - '8081:8080'